name: Publish Images Using fixed version

on:
  push:
    branches:
      - master

jobs:
  publish-notebook:
    name: Publish Jupyter notebook image
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: Log into registry
        run: echo "${{ secrets.FERLAB_DOCKER_HUB_TOKEN }}" | docker login -u ${{ secrets.FERLAB_DOCKER_HUB_USER }} --password-stdin
      - name: Build image spark_331_hadoop_322
        run: docker build -t ferlabcrsj/all-spark-notebook:3.3.1 .
        working-directory: spark_331_hadoop_322
      - name: Push image spark_331_hadoop_322
        run: docker push ferlabcrsj/all-spark-notebook:3.3.1
        working-directory: spark_331_hadoop_322
